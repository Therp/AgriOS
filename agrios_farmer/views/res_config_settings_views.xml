<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">view.res.config.settings.form - agrios_farmer</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Farmers" string="AgriOS" name="agrios">
                    <block title="Operations" name="operations_setting_container">
                        <setting
                            id="farmer_interactions"
                            help="Allows the management of interactions made with farmers (calls, visits, etc...)."
                        >
                            <field name="group_farmer_interactions"/>
                            <div class="content-group">
                                <div invisible="not group_farmer_interactions">
                                    <button
                                        name="%(agrios_farmer.action_farmer_interaction)d"
                                        icon="oi-arrow-right"
                                        type="action"
                                        string="Farmer Interactions"
                                        class="btn-link"
                                    />
                                </div>
                            </div>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

</odoo>
